const myModule = require("../index");

describe("AS Strategy Utils", () => {
  describe("tests", () => {

    // Testing suite dev via strategy
    it("Pass tests", async () => {
      expect(true)
    });
  });
});
